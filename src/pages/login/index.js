import React, { useState } from 'react'

import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native'

const Login = () => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  )
}

const styles = StyleSheet.create({})

export default Login
