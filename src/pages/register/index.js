import React, { useState } from 'react'

import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native'

const Register = () => {
  return (
    <View>
      <Text>Register</Text>
    </View>
  )
}

const styles = StyleSheet.create({})

export default Register
